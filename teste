-- bypasses (walkspeed)
local mt = getrawmetatable(game)
setreadonly(mt,false)
local old = mt.__index
mt.__index = newcclosure(function(self,ww)
    if ww == "WalkSpeed" then
        return game.Players.LocalPlayer.Character.Humanoid.WalkSpeed
    end
    return old(self,ww)
end)

getgenv().walkspeed = -1

local gmt = getrawmetatable(game)
local index = gmt.__newindex
setreadonly(gmt, false)
gmt.__newindex = newcclosure(function(hum, wkspd, val)
	if tostring(hum) == 'Humanoid' and wkspd == 'WalkSpeed' then
		if getgenv().walkspeed ~= -1 then
			return index(hum, wkspd, getgenv().walkspeed)
		end
	end
	return index(hum, wkspd, val)
end)
setreadonly(gmt, true)

local vu = game:GetService("VirtualUser")
	game:GetService("Players").LocalPlayer.Idled:connect(function()
		vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
		wait(1)
		vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
	end)

local Egg = {}

for i,v in pairs(game:GetService("Workspace")._EGGS:GetChildren()) do
	if not v.Name:match("Range") then
		table.insert(Egg, v.Name)
	end
end

local Trial = {}

for i, v in pairs(game:GetService("Workspace")._TRIAL.Easy["Enemies"]:GetDescendants()) do
    if v:IsA "Model" and v.Parent.Parent.Name == "_TRIAL.Easy.Enemies" then
		if not table.find(Trial, tostring(p)) then
			table.insert(Trial, tostring(p))
		end
    end
end
local function getClosest()
    local dist, thing = math.huge
    for i, v in next, game:GetService("Workspace")._TRIAL.Easy["Enemies"]:GetDescendants() do
      if  v.Name == "HumanoidRootPart" then
            local mag =
                (game.Players.LocalPlayer.Character.HumanoidRootPart.Position - v.Position).magnitude
            if mag < dist then
                dist = mag
                thing = v
            end
        end
    end
    return thing
end

local Boss = {}

for i, v in pairs(game:GetService("ReplicatedStorage").Assets.RaidBosses:GetDescendants()) do
    if v:IsA "Model" and v.Parent.Parent.Name == "Assets.RaidBosses" then
		if not table.find(Boss, tostring(u)) then
			table.insert(Boss, tostring(u))
		end
    end
end
local function getClosest()
    local dist, thing = math.huge
    for i, v in next, game:GetService("ReplicatedStorage").Assets.RaidBosses:GetDescendants() do
      if  v.Name == "HumanoidRootPart" then
            local mag =
                (game.Players.LocalPlayer.Character.HumanoidRootPart.Position - v.Position).magnitude
            if mag < dist then
                dist = mag
                thing = v
            end
        end
    end
    return thing
end

local Meteors = {}

for i, v in pairs(game:GetService("Workspace")._METEORS:GetDescendants()) do
    if v:IsA "Model" and v.Parent.Parent.Name == "_METEORS" then
		if not table.find(Meteors, tostring(j)) then
			table.insert(Meteors, tostring(j))
		end
    end
end
local function getClosest()
    local dist, thing = math.huge
    for i, v in next, game:GetService("Workspace")._METEORS:GetDescendants() do
      if  v.Name == "HumanoidRootPart" then
            local mag =
                (game.Players.LocalPlayer.Character.HumanoidRootPart.Position - v.Position).magnitude
            if mag < dist then
                dist = mag
                thing = v
            end
        end
    end
    return thing
end

local Enemies = {}

for i, v in pairs(game:GetService("Workspace")["_ENEMIES"]:GetDescendants()) do
    if v:IsA "Model" and v.Parent.Parent.Name == "_ENEMIES" then
		if not table.find(Enemies, tostring(v)) then
			table.insert(Enemies, tostring(v))
		end
    end
end
local function getClosest()
    local dist, thing = math.huge
    for i, v in next, game:GetService("Workspace")["_ENEMIES"]:GetDescendants() do
      if  v.Name == "HumanoidRootPart" then
            local mag =
                (game.Players.LocalPlayer.Character.HumanoidRootPart.Position - v.Position).magnitude
            if mag < dist then
                dist = mag
                thing = v
            end
        end
    end
    return thing
end
local Plr = game.Players.LocalPlayer
local vu = game:GetService("VirtualUser")
local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/UI-Interface/CustomFIeld/main/RayField.lua'))()
getgenv().SecureMode = true

local Window = Rayfield:CreateWindow({
	Name = "Wolf Hub | BETA",
	LoadingTitle = "Wolf Hub",
	LoadingSubtitle = "By Wolf",
	ConfigurationSaving = {
		Enabled = true,
		FolderName = nil, 
		FileName = "Wolf Hub"
	},
        Discord = {
        	Enabled = false,
        	Invite = "", 
        	RememberJoins = false
        },

})

local update = Window:CreateTab("Update")

local Section = update:CreateSection("Update Script")

local Paragraph = update:CreateParagraph({Title = "Functions Added", Content = "AUTO TRIAL BETA | AUTO EGG"})

local Paragraph = update:CreateParagraph({Title = "Menus Added", Content = "PETS"})

local Paragraph = update:CreateParagraph({Title = "what's coming next update", Content = "AUTO QUEST"})

local Paragraph = update:CreateParagraph({Title = "Testing", Content = "AUTO EGG"})

local Tab = Window:CreateTab("Main")

local Section = Tab:CreateSection("Farm")

local Button = Tab:CreateButton({
	Name = "Hide Name",
	Interact = "APERTAR",
	Callback = function()
local Hum = game:GetService("Players").LocalPlayer.Character.Head.player_tag.frame
Hum.Parent = nil
Hum.Parent = game:GetService("Players").LocalPlayer.Character.Head.player_tag.frame
end
})

Tab:CreateToggle({
	Name = "Auto Attack/Click",
	CurrentValue = false,
	Flag = "Toggle1", 
	Callback = function(Value)
		a = Value
            while a do task.wait()
				local args = {
				[1] = {
					[1] = "Hit",
					[2] = getClosest().Parent
				}
			}

			game:GetService("ReplicatedStorage").Remotes.Server:FireServer(unpack(args))
		end
	end,
})
local Dropdown = Tab:CreateDropdown({
	Name = "Enemies",
	Options = Enemies,
	CurrentOption = "Enemy List",
	Flag = "Dropdown1", 
	Callback = function(Value)
		getgenv().enemy = Value
	end,
})
local function getClosestMob()
    local dist, thing = math.huge
    for i, v in next, game:GetService("Workspace")["_ENEMIES"]:GetDescendants() do
      if  v.Name == "HumanoidRootPart" and v.Parent.Name == enemy then
            local mag =
                (game.Players.LocalPlayer.Character.HumanoidRootPart.Position - v.Position).magnitude
            if mag < dist then
                dist = mag
                thing = v
            end
        end
    end
    return thing
end
local function getClosestMeteor()
    local dist, thing = math.huge
    for i, v in next, game:GetService("Workspace")._METEORS:GetDescendants() do
      if  v.Name == "root" then
            local mag =
                (game.Players.LocalPlayer.Character.HumanoidRootPart.Position - v.Position).magnitude
            if mag < dist then
                dist = mag
                thing = v
            end
        end
    end
    return thing
end

local function getClosestTrial()
    local dist, thing = math.huge
    for i, v in next, game:GetService("Workspace")._TRIAL.Easy["Enemies"]:GetDescendants() do
      if  v.Name == "HumanoidRootPart" then
            local mag =
                (game.Players.LocalPlayer.Character.HumanoidRootPart.Position - v.Position).magnitude
            if mag < dist then
                dist = mag
                thing = v
            end
        end
    end
    return thing
end

local function getClosestBoss()
    local dist, thing = math.huge
    for i, v in next, game:GetService("ReplicatedStorage").Assets.RaidBosses:GetDescendants() do
      if  v.Name == "HumanoidRootPart" and v.Parent.Name == boss then
            local mag =
                (game.Players.LocalPlayer.Character.HumanoidRootPart.Position - v.Position).magnitude
            if mag < dist then
                dist = mag
                thing = v
            end
        end
    end
    return thing
end
Tab:CreateToggle({
	Name = "Auto Mob",
	CurrentValue = false,
	Flag = "Toggle2", 
	Callback = function(Value)
		b = Value
            while b do task.wait()
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = getClosestMob().CFrame
		end
	end,
})

local Label = Tab:CreateLabel("Auto Raid Boss")

local Dropdown = Tab:CreateDropdown({
	Name = "Boss (Select)",
	Options = {"beerus", "chainsaw_man", "eren_final", "gon_adult", "meliodas_demon", "naruto_six_paths", "rimuru", "whitebeard"},
	CurrentOption = "Boss List",
	Flag = "Dropdown2", 
	Callback = function(Value)
		getgenv().boss = Value
	end,
})

Tab:CreateToggle({
	Name = "Auto Boss",
	CurrentValue = false,
	Flag = "Toggle4", 
	Callback = function(boss)
		u = boss
            while u do task.wait()
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = getClosestBoss().CFrame
		end
	end,
})

Tab:CreateToggle({
	Name = "Auto Attack Boss",
	CurrentValue = false,
	Flag = "Toggle2", 
	Callback = function(Value)
		a = Value
            while a do task.wait()
				local args = {
				[1] = {
					[1] = "Hit",
					[2] = getClosestBoss().Parent
				}
			}

			game:GetService("ReplicatedStorage").Remotes.Server:FireServer(unpack(args))
		end
	end,
})

local Label = Tab:CreateLabel("Auto Trial (BETA)")

Tab:CreateToggle({
	Name = "Auto Trial",
	CurrentValue = false,
	Flag = "Toggle5", 
	Callback = function(trial)
		p = trial
            while p do task.wait()
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = getClosestTrial().CFrame
		end
	end,
})

Tab:CreateToggle({
	Name = "Auto Attack Trial",
	CurrentValue = false,
	Flag = "Toggle2", 
	Callback = function(Value)
		a = Value
            while a do task.wait()
				local args = {
				[1] = {
					[1] = "Hit",
					[2] = getClosestTrial().Parent
				}
			}

			game:GetService("ReplicatedStorage").Remotes.Server:FireServer(unpack(args))
		end
	end,
})

local Label = Tab:CreateLabel("Auto Meteor")

Tab:CreateToggle({
	Name = "Auto Meteors",
	CurrentValue = false,
	Flag = "Toggle3", 
	Callback = function(meteor)
		j = meteor
            while j do task.wait()
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = getClosestMeteor().CFrame
		end
	end,
})

Tab:CreateToggle({
	Name = "Auto Attack Meteor",
	CurrentValue = false,
	Flag = "Toggle2", 
	Callback = function(Value)
		a = Value
            while a do task.wait()
				local args = {
				[1] = {
					[1] = "Hit",
					[2] = getClosestMeteor().Parent
				}
			}

			game:GetService("ReplicatedStorage").Remotes.Server:FireServer(unpack(args))
		end
	end,
})

local pets = Window:CreateTab("Fighters")

local Pets = pets:CreateSection("Fighters")

local Dropdown = pets:CreateDropdown({
	Name = "Fighters Egg",
	Options = Egg,
	CurrentOption = "Egg List",
	Flag = "EggList", 
	Callback = function(Option)
		getgenv().SelectedEgg = Option
	end,
})

pets:CreateToggle({
	Name = "Auto Open Egg",
	CurrentValue = false,
	Flag = "AutoEgg", 
	Callback = function(egg)
		ege = egg
            while ege do task.wait(0.2)
local eggs = 
{
	[1] = "BuyHeroes", 
	[2] = SelectedEgg
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(eggs)
		end
	end,
})

local Loja = Window:CreateTab("Shop")

local loja = Loja:CreateSection("Shop")

local Label = Loja:CreateLabel("Auto Skills")

Loja:CreateToggle({
	Name = "Auto Skills Loop",
	CurrentValue = false,
	Flag = "nada", 
	Callback = function(Auto)
	auto = Auto
            while auto do task.wait(1)
local A_1 = 
{
	[1] = "SkillSpin"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
        end
    end,
})

local Button = Loja:CreateButton({
	Name = "Auto Skills (NO SPAM)",
	Interact = "APERTAR",
	Callback = function()
local A_1 = 
{
	[1] = "SkillSpin"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
		end
})

local Button = Loja:CreateButton({
	Name = "Teleport to Skill",
	Interact = "APERTAR",
	Callback = function() 
game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-400.655334, 3.51512766, 816.332947, 0.878142238, -0, -0.478399724, 0, 1, -0, 0.478399724, 0, 0.878142238))
	end
})

local Label = Loja:CreateLabel("Auto Swords")

Loja:CreateToggle({
	Name = "Auto Sword Loop",
	CurrentValue = false,
	Flag = "nada", 
	Callback = function(Auto)
	auto = Auto
            while auto do task.wait()
local A_1 = 
{
	[1] = "Swords"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
        end
    end,
})

local Button = Loja:CreateButton({
	Name = "Buy Sword",
	Interact = "APERTAR",
	Callback = function()
local A_1 = 
{
	[1] = "Swords"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
		end
})

local Label = Loja:CreateLabel("Auto Class")

Loja:CreateToggle({
	Name = "Auto Class Loop",
	CurrentValue = false,
	Flag = "nada", 
	Callback = function(Auto)
	auto = Auto
            while auto do task.wait()
local A_1 = 
{
	[1] = "Class"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
        end
	end,
})

local Button = Loja:CreateButton({
	Name = "Buy Class",
	Interact = "APERTAR",
	Callback = function()
local A_1 = 
{
	[1] = "Class"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
		end
})

local Label = Loja:CreateLabel("Craft")

local Button = Loja:CreateButton({
	Name = "Teleport to Crafts",
	Interact = "TELEPORTAR",
	Callback = function()
game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-15319.876, 4.50730515, 1536.82739, -1, 0, 0, 0, 1, 0, 0, 0, -1))
		end
})

local Potion = Window:CreateTab("Potion")

local Potions = Potion:CreateSection("Potions")

local Button = Potion:CreateButton({
	Name = "All Potions (X1)",
	Interact = "APERTAR",
	Callback = function() 
local A_1 = 
{
	[1] = "Potions", 
	[2] = "energy"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
local A_1 = 
{
	[1] = "Potions", 
	[2] = "damage"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
local A_1 = 
{
	[1] = "Potions", 
	[2] = "souls"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
local A_1 = 
{
	[1] = "Potions", 
	[2] = "lucky"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
	end
})

Potion:CreateToggle({
	Name = "Auto Potions Loop (ALL)",
	CurrentValue = false,
	Flag = "seila", 
	Callback = function(potion)
	Potions = potion
            while Potions do task.wait()
    local A_1 = 
{
	[1] = "Potions", 
	[2] = "energy"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
local A_1 = 
{
	[1] = "Potions", 
	[2] = "damage"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
local A_1 = 
{
	[1] = "Potions", 
	[2] = "souls"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
local A_1 = 
{
	[1] = "Potions", 
	[2] = "lucky"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
	end
end
})

local Button = Potion:CreateButton({
	Name = "Potions Energy (X1)",
	Interact = "APERTAR",
	Callback = function() 
local A_1 = 
{
	[1] = "Potions", 
	[2] = "energy"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
	end
})

local Button = Potion:CreateButton({
	Name = "Potions Damage (X1)",
	Interact = "APERTAR",
	Callback = function() 
local A_1 = 
{
	[1] = "Potions", 
	[2] = "damage"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
	end
})

local Button = Potion:CreateButton({
	Name = "Potions Souls (X1)",
	Interact = "APERTAR",
	Callback = function() 
local A_1 = 
{
	[1] = "Potions", 
	[2] = "souls"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
	end
})

local Button = Potion:CreateButton({
	Name = "Potions Lucky (X1)",
	Interact = "APERTAR",
	Callback = function() 
local A_1 = 
{
	[1] = "Potions", 
	[2] = "lucky"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
	end
})

local teleport = Window:CreateTab("Teleport")

local tele = teleport:CreateSection("Teleports")

local Label = teleport:CreateLabel("need the island")

local Button = teleport:CreateButton({
	Name = "Pyecy Vallage",
	Interact = "TELEPORTAR",
	Callback = function()
local A_1 = 
{
	[1] = "Teleport", 
	[2] = "1"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
wait(2)
game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-327.911224, 2.59337974, 791.630615, -1, 0, 0, 0, 1, 0, 0, 0, -1))
		end
})
local Button = teleport:CreateButton({
	Name = "Leaf Village",
	Interact = "TELEPORTAR",
	Callback = function()
	local A_1 = 
{
	[1] = "Teleport", 
	[2] = "2"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
wait(2)
            game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-3344.32617, -5.57830048, 688.240967, -1, 0, 0, 0, 1, 0, 0, 0, -1))
		end
})
local Button = teleport:CreateButton({
	Name = "Planet Nomak",
	Interact = "TELEPORTAR",
	Callback = function()
	local A_1 = 
{
	[1] = "Teleport", 
	[2] = "3"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
wait(2)
            game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-6422.69873, -9.16890907, 912.840088, -0.535309672, 0, 0.844656229, 0, 1, 0, -0.844656229, 0, -0.535309672))
		end
})
local Button = teleport:CreateButton({
	Name = "Titan District",
	Interact = "TELEPORTAR",
	Callback = function()
	local A_1 = 
{
	[1] = "Teleport", 
	[2] = "4"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
wait(2)
            game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-9440.08398, -9.05495262, 526.021362, 0.997998178, 0, 0.0632423609, 0, 1, 0, -0.0632423609, 0, 0.997998178))
		end
})
local Button = teleport:CreateButton({
	Name = "Hunter City",
	Interact = "TELEPORTAR",
	Callback = function()
	local A_1 = 
{
	[1] = "Teleport", 
	[2] = "5"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
wait(2)
            game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-12515.3721, -2.459728, 1037.77637, 0, 0, 1, 0, 1, -0, -1, 0, 0))
		end
})
local Button = teleport:CreateButton({
	Name = "Sins Kingdom",
	Interact = "TELEPORTAR",
	Callback = function()
	local A_1 = 
{
	[1] = "Teleport", 
	[2] = "6"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
wait(2)
            game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-15647.7393, 5.86402702, 1438.17493, 0, 0, -1, 0, 1, 0, 1, 0, 0))
		end
})
local Button = teleport:CreateButton({
	Name = "Slime Forest",
	Interact = "TELEPORTAR",
	Callback = function()
	local A_1 = 
{
	[1] = "Teleport", 
	[2] = "7"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
wait(2)
            game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-18256.3438, 4.72961473, 604.605713, 0.766061246, -0, -0.642767608, 0, 1, -0, 0.642767608, 0, 0.766061246))
		end
})
local Button = teleport:CreateButton({
	Name = "Chinsaw City",
	Interact = "TELEPORTAR",
	Callback = function()
	local A_1 = 
{
	[1] = "Teleport", 
	[2] = "8"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
wait(2)
            game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-21228.041, -0.750942409, 938.035461, 1, 0, 0, 0, 1, 0, 0, 0, 1))
		end
})
local Button = teleport:CreateButton({
	Name = "Slayer Village",
	Interact = "TELEPORTAR",
	Callback = function()
	local A_1 = 
{
	[1] = "Teleport", 
	[2] = "9"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
wait(2)
            game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-24489.9453, -7.412323, 722.017212, 0.51043582, 0, 0.859915853, 0, 1, 0, -0.859915853, 0, 0.51043582))
		end
})

local Misc = Window:CreateTab("Misc")

local misc = Misc:CreateSection("Misc")

local Label = Misc:CreateLabel("Server")

local Button = Misc:CreateButton({
	Name = "Redeem All Codes",
	Interact = "APERTAR",
	Callback = function()
local A_1 = 
{
	[1] = "Codes", 
	[2] = "UPDATE5"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
local A_1 = 
{
	[1] = "Codes", 
	[2] = "40KLIKES"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
	end
})

local Button = Misc:CreateButton({
	Name = "Group Rewards",
	Interact = "RESGATAR",
	Callback = function()
local A_1 = 
{
	[1] = "GroupRewards"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
		end
})

local Button = Misc:CreateButton({
	Name = "Daily Rewards",
	Interact = "RESGATAR",
	Callback = function()
local A_1 = 
{
	[1] = "DailyRewards", 
	[2] = "1",
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
		end
})

local Button = Misc:CreateButton({
	Name = "Reset Daily",
	Interact = "RESGATAR",
	Callback = function()
local A_1 = 
{
	[1] = "DailyRewards", 
	[2] = "reset"
}
local Event = game:GetService("ReplicatedStorage").Remotes.Server
Event:FireServer(A_1)
		end
})

local Button = Misc:CreateButton({
	Name = "Server Hop",
	Interact = "APERTAR",
	Callback = function()
    local module = loadstring(game:HttpGet"https://raw.githubusercontent.com/LeoKholYt/roblox/main/lk_serverhop.lua")()
module:Teleport(game.PlaceId)
	end
})

local Button = Misc:CreateButton({
	Name = "Reentrar (FORCE RESET)",
	Interact = "APERTAR",
	Callback = function()
	game:GetService("TeleportService"):Teleport(11542692507, game:GetService("Players").LocalPlayer)
	end
})

local Label = Misc:CreateLabel("Player")

local Slider = Misc:CreateSlider({
	Name = "WalkSpeed",
	Range = {0, 200},
	Increment = 20,
	Suffix = "walkspeed",
	CurrentValue = 20,
	Flag = "Slider1",
	Callback = function(walk)
		getgenv().walkspeed = walk
	end,
})

local Button = Misc:CreateButton({
	Name = "Anti Afk",
	Interact = "APERTAR",
	Callback = function(vu)
local vu = game:GetService("VirtualUser")
	game:GetService("Players").LocalPlayer.Idled:connect(function()
		vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
		wait(1)
		vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
	end)
		end
})

local Label = Misc:CreateLabel("Works when you fall into void")

local Button = Misc:CreateButton({
	Name = "Anti Kick V1",
	Interact = "APERTAR",
	Callback = function()
assert(getrawmetatable)
grm = getrawmetatable(game)
setreadonly(grm, false)
old = grm.__namecall
grm.__namecall = newcclosure(function(self, ...)
    local args = {...}
    if tostring(args[1]) == "TeleportDetect" then
        return
    elseif tostring(args[1]) == "CHECKER_1" then
        return
    elseif tostring(args[1]) == "CHECKER" then
        return
    elseif tostring(args[1]) == "GUI_CHECK" then
        return
    elseif tostring(args[1]) == "OneMoreTime" then
        return
    elseif tostring(args[1]) == "checkingSPEED" then
        return
    elseif tostring(args[1]) == "BANREMOTE" then
        return
    elseif tostring(args[1]) == "PERMAIDBAN" then
        return
    elseif tostring(args[1]) == "KICKREMOTE" then
        return
    elseif tostring(args[1]) == "BR_KICKPC" then
        return
    elseif tostring(args[1]) == "BR_KICKMOBILE" then
        return
    end
    return old(self, ...)
end)
print("ANTI KIKC V1 LOADED")
		end
})

local Button = Misc:CreateButton({
	Name = "Anti Kick V2",
	Interact = "APERTAR",
	Callback = function()
loadstring(game:HttpGet("https://pastebin.com/raw/gsxvWvnj"))()
print("ANTI KIKC V2 LOADED")
		end
})

local fps = Window:CreateTab("Fps/Lag")

local Fps = fps:CreateSection("Fps")

local Label = fps:CreateLabel("Fps Boost/just to activate")

local Button = fps:CreateButton({
    Name = "FPS Boost",
    Interact = "APERTAR",
    Callback = function()
local decalsyeeted = true
local g = game
local w = g.Workspace
local l = g.Lighting
local t = w.Terrain
sethiddenproperty(l,"Technology",2)
sethiddenproperty(t,"Decoration",false)
t.WaterWaveSize = 0
t.WaterWaveSpeed = 0
t.WaterReflectance = 0
t.WaterTransparency = 0
l.GlobalShadows = false
l.FogEnd = 9e9
l.Brightness = 0
settings().Rendering.QualityLevel = "Level01"
for i, v in pairs(g:GetDescendants()) do
    if v:IsA("Part") or v:IsA("Union") or v:IsA("CornerWedgePart") or v:IsA("TrussPart") then
        v.Material = "Plastic"
        v.Reflectance = 0
    elseif v:IsA("Decal") or v:IsA("Texture") and decalsyeeted then
        v.Transparency = 1
    elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
        v.Lifetime = NumberRange.new(0)
    elseif v:IsA("Explosion") then
        v.BlastPressure = 1
        v.BlastRadius = 1
    elseif v:IsA("Fire") or v:IsA("SpotLight") or v:IsA("Smoke") or v:IsA("Sparkles") then
        v.Enabled = false
    elseif v:IsA("MeshPart") then
        v.Material = "Plastic"
        v.Reflectance = 0
        v.TextureID = 10385902758728957
    end
end
for i, e in pairs(l:GetChildren()) do
    if e:IsA("BlurEffect") or e:IsA("SunRaysEffect") or e:IsA("ColorCorrectionEffect") or e:IsA("BloomEffect") or e:IsA("DepthOfFieldEffect") then
        e.Enabled = false
    end
end
end
})

local Button = fps:CreateButton({
    Name = "FPS Boost V2",
    Interact = "APERTAR",
    Callback = function()
local a = game
local b = a.Workspace
local c = a.Lighting
local d = b.Terrain
d.WaterWaveSize = 0
d.WaterWaveSpeed = 0
d.WaterReflectance = 0
d.WaterTransparency = 0
c.GlobalShadows = false
c.FogEnd = 9e9
c.Brightness = 0
settings().Rendering.QualityLevel = "Level01"
for e, f in pairs(a:GetDescendants()) do
   if f:IsA("Part") or f:IsA("Union") or f:IsA("CornerWedgePart") or f:IsA("TrussPart") then
       f.Material = "Plastic"
       f.Reflectance = 0
   elseif f:IsA("Decal") or f:IsA("Texture") then
       f.Transparency = 0
   elseif f:IsA("ParticleEmitter") or f:IsA("Trail") then
       f.Lifetime = NumberRange.new(0)
   elseif f:IsA("Explosion") then
       f.BlastPressure = 0
       f.BlastRadius = 0
   elseif f:IsA("Fire") or f:IsA("SpotLight") or f:IsA("Smoke") or f:IsA("Sparkles") then
       f.Enabled = false
   elseif f:IsA("MeshPart") then
       f.Material = "Plastic"
       f.Reflectance = 0
       f.TextureID = 10385902758728957
   end
end
for e, g in pairs(c:GetChildren()) do
   if
       g:IsA("BlurEffect") or g:IsA("SunRaysEffect") or g:IsA("ColorCorrectionEffect") or g:IsA("BloomEffect") or
           g:IsA("DepthOfFieldEffect")
    then
       g.Enabled = false
   end
end
sethiddenproperty(game.Lighting, "Technology", "Compatibility")
end
})

local Label = Misc:CreateLabel("Script")

local Button = Misc:CreateButton({
	Name = "Destroy UI",
	Interact = "APERTAR",
	Callback = function()
    Rayfield:Destroy()
	end
})

local Creditos = Window:CreateTab("Credits")

local creditos = Creditos:CreateSection("Creditos")

local Label = Creditos:CreateLabel("Script by: KILLUA")
local Label = Creditos:CreateLabel("Functions by: Thzin YT")
local Label = Creditos:CreateLabel("Lib/UI: RayField")
